# Frontend (optional — defaults to /api which works on Vercel and local dev)
VITE_API_URL=/api

# Backend — set these in Vercel Dashboard → Settings → Environment Variables
MONGODB_URI=mongodb+srv://faizaanalam:YOUR_PASSWORD@cryptotrak1.c6qsslk.mongodb.net/cryptotrack?retryWrites=true&w=majority&appName=cryptotrak1
JWT_SECRET=change-this-to-a-long-random-string

# Optional — Vercel sets VERCEL_URL automatically; only override for custom domains
# API_URL=https://your-app.vercel.app
# FRONTEND_URL=https://your-app.vercel.app

# Optional — Google sign-in
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=